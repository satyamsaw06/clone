pipeline{
  agent {
    label "built-in"
    Workspace "/mnt/contain"
  } 
      stages{
            stage("1st"){
              steps{
                sh "cat index.html"
              }
            }
      }
}
